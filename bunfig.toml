[install]
# Use exact versions for better reproducibility
exact = true

[install.scopes]
# Configure scopes for workspaces

[test]
# Configure test environment
preload = ["./test/setup.ts"]

[run]
# Configure run environment
bun = true 